import React from "react";
import { withStyles } from "material-ui/styles";
const styles = theme => ({});
const StreetView = props => {
  const { classes } = props;
  return <div className={classes}> StreetView </div>;
};
export default withStyles(styles)(StreetView);
